#include <bits/stdc++.h>

using namespace std;
using ll  = long long;
using pair = Pair<ll ,ll>; 
using vec =  vetor<pair>;



int main(){
  int N,Q;
  pair p; 
  vec pairs;
  
  
  for(int i{}; i < N; ++i){
    cin >> p.first >> p.second;
  }
  cin >> Q;
  
  vector<ll> T;
   for(int i{}; i < Q; ++i){
    ll Ti;
    cin >> Ti;
    T.push_back(Ti);
  }
    //**
 // std::sort(vec.begin(), vec.end(), [](const auto& a, const auto& b) {
  //  return a.second < b.second;
//});

  
  
  
  for(int i{}; i < Q; ++i){
    
  }
  
  
  return 0;
}